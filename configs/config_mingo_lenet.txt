[model]
base: 'VanillaLeNet'
nrot: None
early_stopping: True
quiet: False

[data]
dataset: 'MLFR'
datadir: 'mingo'
datamean: 0.0040
datastd: 0.0401

[training]
gpu: False			# use gpu? [T/F]
batch_size: 50                  # samples per minibatch
frac_val: 0.2                   # fraction of training set for validation
epochs: 600                     # total number of epochs
imsize: 150                     # pixels on side of image
num_classes: 2                  # number of target classes
lr0: 0.0001                     # initial learning rate
decay: 0.000001                 # weight decay

[output]
csvfile: 'mingo_lenet.csv'
modfile: 'mingo_lenet.pt'

[metrics]
outputdir: 'mingo_lenet' 
